import { prop } from 'ramda';

export const loggedIn = prop('loggedIn');

export const loggingIn = prop('loggingIn');

export const username = prop('username');

export const poesessid = prop('poesessid');
